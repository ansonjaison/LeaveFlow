package com.example.leaveflow.enums;

/**
 * Represents the two roles in the system.
 * Used on the Employee entity to distinguish admins from regular employees.
 */
public enum Role {
    ADMIN,
    EMPLOYEE
}
